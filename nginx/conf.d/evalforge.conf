# HTTP: serve ACME challenges + redirect everything else to HTTPS
server {
    listen 80;
    server_name evalforge.com www.evalforge.com;

    # ACME HTTP-01 challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # return 301 https://$host$request_uri;
        proxy_pass http://app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS: main app (Commented out for local testing without certs)
# server {
#     listen 443 ssl http2;
#     server_name evalforge.com www.evalforge.com;
#
#     ssl_certificate /etc/letsencrypt/live/evalforge.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/evalforge.com/privkey.pem;
#
#     # Basic security / proxy headers
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:10m;
#
#     location / {
#         proxy_pass http://app:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
