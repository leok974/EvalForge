-- sql-insert-update-delete
-- TASK:
-- The Archive is a living entity. Data must be added, corrected, and occasionally purged.
--
-- Requirements:
-- 1) INSERT a new user: id=7, name='Grace', email='grace@example.com', age=30, city='Berlin', is_active=1
-- 2) UPDATE Bob's (id=2) city to 'London'.
-- 3) DELETE the order with id=4.
--
-- Rules:
-- - You can write multiple statements separated by semicolons.
-- - For the final result, SELECT * FROM users; to show the changes.
--
-- TODO:
-- Write the mutation statements followed by a SELECT.

SELECT * FROM users;