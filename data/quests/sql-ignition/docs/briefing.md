# Briefing

Welcome to the Archives. Before we can perform complex analysis, we must ensure your connection to the database is active.

## Mission

Perform a basic "dump" of the `users` table to verify you can retrieve records.

## Requirements

1.  **Columns**: [SELECT](glossary/sql/select) all columns using the `*` symbol.
2.  **Table**: Retrieve data [FROM](glossary/sql/from) the `users` [table](glossary/sql/table).

## Success Criteria

-   Every column of the `users` table is returned.
-   The query executes without errors.
