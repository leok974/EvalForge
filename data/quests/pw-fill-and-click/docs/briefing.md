# Briefing: Fill and Click

The CMS login requires entering credentials and clicking Submit. Your task: automate this form interaction using `fill()` and `click()`, then verify you land on the dashboard.

The credentials are `admin` / `secret123`. After successful login, the server redirects to `/dashboard`.
