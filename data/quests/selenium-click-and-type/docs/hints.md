# Hints

Need a little help? Here are a few tips to guide you:

1. Look back at the earlier quests if you need a refresher on `find_element` and `send_keys`.
2. Don't forget that `.click()` is how you interact with button elements.
3. Make sure to use the exact `data-testid` values provided in the instructions.
