# Briefing: Network Intercept

Playwright can intercept any network request and return a custom response — without the request ever reaching the server. Your task: intercept the `/search` route and return a page containing the text "INTERCEPTED".

This technique is powerful for testing error states, offline behavior, and mocked API responses.
