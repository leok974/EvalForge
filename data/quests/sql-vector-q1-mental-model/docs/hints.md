## The Target Table

The table name is `historical_fragments`.

## The Filter

You need to add `1` after the `=` in the WHERE clause.

## Missing Columns

Make sure you include the `embedding` column in your SELECT list.
