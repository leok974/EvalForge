# Briefing: Test Grouping

Real test suites organize tests into logical groups with shared setup. Your task: use `test.describe` to group dashboard tests and `test.beforeEach` to run login before every test case.

Two test cases must pass: one that verifies the dashboard title, and another that checks the navigation links.
