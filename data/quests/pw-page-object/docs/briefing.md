# Briefing: Page Object Model

The Page Object Model (POM) is a design pattern that encapsulates page interactions into reusable classes. Instead of scattering locators and actions across tests, you centralize them in a class per page.

Your task: implement a `LoginPage` class with a `login()` method, then use it in a test to authenticate and reach the dashboard.
