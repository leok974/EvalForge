## Relational Filter

The WHERE clause should be `WHERE category = 'Astronomy'`.

## Vector Similarity

The target vector literal is `'[0.9, 0.1, 0.1]'::vector`.

## Top-1 Result

Make sure you include `LIMIT 1` to find just the 'most' similar fragment.
