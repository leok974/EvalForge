"The archives are vast," the Archivist warned. "A pure vector search can sometimes surface truths from the wrong universe. Hybrid search is the key: filter by category first to build a subset, and THEN search the vector space within that subset. Accuracy meets speed."

You will need to use a standard WHERE clause and then rank the results by their distance metrics to narrow the search space to the relevant domain.
