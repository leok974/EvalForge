# React Ignition

Edit `task.mjs` to export a component named `App`.

Requirements:
1. Render a `div` element.
2. The `div` must have a `data-testid="welcome"` prop.
3. The content of the `div` must be "Hello React".

Do not use JSX. Use `React.createElement`.
