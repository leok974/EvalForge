## Hint 1
Use `range(3, 0, -1)` to get the numbers 3, 2, 1 in descending order.

## Hint 2
Inside the loop, use an f-string to format each line:
`print(f"T-minus {i}")`

## Hint 3
After the loop ends, print `"IGNITION"` as a separate statement — it runs once,
after all countdown lines are done.
