[
    {
        "text": "Your query needs a `WHERE` clause. You are looking for the row where `name = 'Bob'`."
    },
    {
        "text": "The syntax is: `UPDATE employees SET salary = 125000 WHERE name = 'Bob';`"
    },
    {
        "text": "Don't forget to run a `SELECT` statement after the `UPDATE` so the system can grade your result!"
    }
]
