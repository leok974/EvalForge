# Redirection

Generate a report file using output redirection:

1.  `outputs/report.txt` must contain exactly:
    *   First line: `HEADER`
    *   Then all lines of `fixtures/data.txt`
    *   Last line: `FOOTER`
