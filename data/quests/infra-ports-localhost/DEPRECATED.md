# DEPRECATED — infra-ports-localhost

This directory is deprecated and kept only to avoid confusion from older references.

✅ Use the canonical core quest instead:

- `infra-ports-and-localhost` (in `data/questpacks/infra_core.json`)

Reason:
- Naming drift; canonical quest covers the same learning goal with stable tests + runner wiring.

Next:
- Do not add this slug to any questpack.
- If you find links pointing here, redirect them to `infra-ports-and-localhost`.
