# Lists & Tables

Edit `index.html`.

Requirements:
1) `<ul data-testid="task-list">` with exactly 3 `<li>` items:
   - Learn
   - Build
   - Ship
2) `<table data-testid="scores">` with:
   - headers: Name, Score
   - rows: Alice 10; Bob 8
