## Hint 1
Variable names are case-sensitive. The grader checks for exactly `message` —
not `Message`, not `msg`.

## Hint 2
The string value must match exactly: `"System Online"` with a capital S and O,
and a space between the words.
