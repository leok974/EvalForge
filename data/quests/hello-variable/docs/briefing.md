# Hello Variable

Your mission: store a text value in a variable and print it.

Define a variable called `message` and assign it the string `"System Online"`.
Then print it.

Variables are named containers — they let you store a value once and refer to
it by name throughout your program. This quest establishes that foundation.

**Objective:** Assign `message = "System Online"` and print `message`.
