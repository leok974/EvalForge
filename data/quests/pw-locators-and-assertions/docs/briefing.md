# Briefing: Locators and Assertions

The CMS login page has several interactive elements. Your mission: use Playwright's semantic locators to find the heading, the username input, and the submit button — then assert each is visible.

Playwright favors locators that mirror how users perceive the page: by role, by text, by label, or by test ID. Avoid brittle CSS selectors when semantic alternatives exist.
