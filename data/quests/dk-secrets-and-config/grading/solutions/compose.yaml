version: "3.9"
services:
  api:
    image: my-api:latest
    env_file:
      - .env
    environment:
      DB_HOST: db

  db:
    image: postgres:15
    env_file:
      - .env
    environment:
      POSTGRES_DB: app
