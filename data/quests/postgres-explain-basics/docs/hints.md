## Hint 1
EXPLAIN is a keyword that prefixes a SELECT statement. It does not change the query — it only shows the plan.

## Hint 2
The syntax is simply: `EXPLAIN SELECT ...` — put EXPLAIN at the very start of the line, before SELECT.

## Hint 3
The starter code already has the SELECT you need. Just add `EXPLAIN` before `SELECT * FROM audit_logs ...`.
