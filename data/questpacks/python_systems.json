[
    {
        "slug": "python-data-forge",
        "id": 5,
        "title": "Data Forge",
        "world_id": "world-python",
        "track_id": "python-boss",
        "description": "Load a sales CSV, compute revenue per item using qty×price, and return the top sellers.",
        "short_description": "Load sales.csv, aggregate revenue by item, and return the top-k sellers.",
        "summary": "Load a sales CSV, compute revenue per item, and rank the top sellers.",
        "key_terms": [
            "glossary/python/dictionary",
            "glossary/python/iteration",
            "glossary/python/typing"
        ],
        "language": "python",
        "xp_base": 70,
        "starter_code": "from __future__ import annotations\nfrom pathlib import Path\n\n\ndef load_sales(path: str | Path) -> list[dict]:\n  raise NotImplementedError(\"TODO: implement load_sales(path)\")\n\n\ndef revenue_by_item(rows: list[dict]) -> dict[str, float]:\n  raise NotImplementedError(\"TODO: implement revenue_by_item(rows)\")\n\n\ndef top_items(revenue: dict[str, float], k: int) -> list[tuple[str, float]]:\n  raise NotImplementedError(\"TODO: implement top_items(revenue, k)\")\n\n\ndef main() -> None:\n  here = Path(__file__).resolve()\n  path = here.parent / \"fixtures\" / \"sales.csv\"\n  rows = load_sales(path)\n  rev = revenue_by_item(rows)\n  top2 = top_items(rev, 2)\n  for name, value in top2:\n    print(f\"{name}={value:.2f}\")\n\n\nif __name__ == \"__main__\":\n  main()\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-data-forge/workspace"
        },
        "grading_json": {
            "entrypoint": "main.py"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **data pipeline** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-loop",
        "id": 4,
        "title": "Loop",
        "world_id": "world-python",
        "track_id": "python-foundry",
        "description": "Implement generate_evens(limit) — return a list of all even integers from 2 to limit using a for loop.",
        "short_description": "Return all even integers from 2 to limit using a for loop.",
        "summary": "Write generate_evens(limit) — collect even integers into a list and return it.",
        "key_terms": [
            "glossary/python/iteration",
            "glossary/python/variable",
            "glossary/python/print"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "def main() -> None:\n    # Print even numbers from 1..10 inclusive, each on its own line.\n    # Then print the summary line: EVEN_COUNT=5\n    pass\n\nif __name__ == \"__main__\":\n    main()\n",
        "objectives": [
            {
                "id": "stdout_even_numbers",
                "title": "Prints even numbers 2..10 comma-separated",
                "kind": "stdout_regex",
                "rule": {
                    "pattern": "^2,4,6,8,10\\s*$",
                    "flags": "m"
                }
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-loop/workspace"
        },
        "grading_json": {
            "entrypoint": "task.py"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **for loop** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-systems-service-boundaries",
        "id": 70,
        "title": "Service Boundaries & Contracts",
        "world_id": "world-python",
        "track_id": "python-systems",
        "description": "Implement an in-memory ticket store with a service boundary: InMemoryTicketRepo stores Ticket objects, and close_ticket updates a ticket's status through the repository.",
        "short_description": "Implement InMemoryTicketRepo and close_ticket to manage ticket lifecycle through a clean service boundary.",
        "briefing_md": "Implement a small in-memory ticket store. A `Ticket` is a frozen dataclass with an `id` (int) and a `status` (`\"open\"` or `\"closed\"`). `InMemoryTicketRepo` holds tickets in a dictionary and exposes `get(ticket_id)` and `save(ticket)`. The top-level `close_ticket(repo, ticket_id)` loads a ticket, sets its status to `\"closed\"`, saves it, and returns it — raising `KeyError` if the ticket does not exist.",
        "summary": "Implement an in-memory ticket store with a service boundary: InMemoryTicketRepo stores Ticket objects, and close_ticket updates a ticket's status through the repository.",
        "key_terms": [
            "glossary/python/function",
            "glossary/python/exception",
            "glossary/python/typing"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Dict, Optional\n\n\n@dataclass(frozen=True)\nclass Ticket:\n    id: int\n    status: str  # \"open\" or \"closed\"\n\n\nclass InMemoryTicketRepo:\n    def __init__(self, initial: Optional[Dict[int, Ticket]] = None):\n        self._db: Dict[int, Ticket] = dict(initial or {})\n\n    def get(self, ticket_id: int) -> Optional[Ticket]:\n        # TODO: return the Ticket with the given id, or None if not found\n        raise NotImplementedError(\"TODO: implement get(ticket_id)\")\n\n    def save(self, ticket: Ticket) -> None:\n        # TODO: store ticket in self._db keyed by ticket.id\n        raise NotImplementedError(\"TODO: implement save(ticket)\")\n\n\ndef close_ticket(repo: InMemoryTicketRepo, ticket_id: int) -> Ticket:\n    # TODO: load the ticket (raise KeyError if not found)\n    # TODO: create a new Ticket with status=\"closed\" and save it\n    # TODO: return the closed ticket\n    raise NotImplementedError(\"TODO: implement close_ticket(repo, ticket_id)\")\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "hint": "Implement `get()` to look up tickets by id (return None if missing), `save()` to store a Ticket keyed by its id, and `close_ticket()` to load, update status to `\"closed\"`, save, and return the ticket — raising KeyError if it doesn't exist.",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-systems-service-boundaries/workspace"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **separation of concerns** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-systems-resilient-job-runner",
        "id": 71,
        "title": "Resilient Job Runner",
        "world_id": "world-python",
        "track_id": "python-systems",
        "description": "Build a retry-capable job runner that handles transient failures and fatal errors with a deterministic retry policy.",
        "short_description": "Build a job runner with retry logic: up to 3 attempts for transient errors, immediate abort for fatal ones.",
        "briefing_md": "Implement `run_jobs` to execute a list of jobs with a retry policy: up to 3 attempts for `TransientError`, immediately stop on `FatalError`. Return a list of result dicts with `id`, `ok`, `attempts`, `value`, and `error` fields.",
        "summary": "Build a retry-capable job runner that handles transient failures and fatal errors with a deterministic retry policy.",
        "key_terms": [
            "glossary/python/exception",
            "glossary/python/iteration",
            "glossary/python/function"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any\n\n\nclass TransientError(Exception):\n    pass\n\n\nclass FatalError(Exception):\n    pass\n\n\ndef coerce_id(value: Any) -> int:\n    try:\n        return int(str(value).strip())\n    except Exception:\n        return 0\n\n\ndef execute_job(job: dict, attempt: int) -> int:\n    \"\"\"\n    Execute a single job attempt.\n\n    Kinds:\n      - add: always succeeds\n      - flaky_add: fails TransientError for first `fail_times` attempts\n      - fatal: always fails FatalError\n    \"\"\"\n    # TODO: Implement job execution logic\n    pass\n\n\ndef run_jobs(jobs: list[dict]) -> list[dict]:\n    \"\"\"\n    Pure core runner. Does not print, does not read files.\n\n    Retry policy:\n      - max_attempts = 3\n      - retry only TransientError\n      - FatalError is never retried\n    \"\"\"\n    results: list[dict] = []\n    max_attempts = 3\n    \n    # TODO: Implement retry loop\n    \n    return results\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-systems-resilient-job-runner/workspace"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **retry** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-systems-observability-sli",
        "id": 72,
        "title": "Observability & SLIs",
        "world_id": "world-python",
        "track_id": "python-systems",
        "description": "Implement calculate_availability(events) to measure the fraction of HTTP requests with successful status codes (200–399), returned as a float rounded to 4 decimal places.",
        "short_description": "Implement calculate_availability(events) to return the fraction of HTTP requests with 2xx/3xx status codes.",
        "briefing_md": "Implement `calculate_availability(events)`. It receives a list of event dicts, each with a `status_code` key. A response is successful if `200 <= status_code <= 399`. Return the ratio of successes to total events as a `float` rounded to 4 decimal places.",
        "summary": "Implement calculate_availability(events) to measure the fraction of HTTP requests with successful status codes (200–399), returned as a float rounded to 4 decimal places.",
        "key_terms": [
            "glossary/python/dictionary",
            "glossary/python/iteration",
            "glossary/python/function"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "from __future__ import annotations\n\nimport math\nfrom typing import Any\n\n\ndef _is_success(status: Any) -> bool:\n    return isinstance(status, int) and 200 <= status <= 399\n\n\ndef _is_5xx(status: Any) -> bool:\n    return isinstance(status, int) and 500 <= status <= 599\n\n\ndef _p95_nearest_rank(latencies: list[int]) -> int:\n    if not latencies:\n        return 0\n    xs = sorted(latencies)\n    n = len(xs)\n    k = math.ceil(0.95 * n)\n    return xs[k - 1]\n\n\ndef compute_sli_report(events: list[dict]) -> dict:\n    \"\"\"\n    Pure function: compute a deterministic SLI report from raw events.\n\n    Must not print or read files.\n    \"\"\"\n    # TODO: Implement metrics per README.\n    #\n    # Return shape:\n    # {\n    #   \"total_requests\": int,\n    #   \"success_rate\": float,   # rounded to 3 decimals\n    #   \"error_rate\": float,     # rounded to 3 decimals (5xx only)\n    #   \"p95_latency_ms\": int,\n    #   \"slo_ok\": bool,\n    #   \"routes\": [\n    #       {\"route\": str, \"requests\": int, \"errors_5xx\": int, \"avg_latency_ms\": int},\n    #       ...\n    #   ]\n    # }\n    return {}\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-systems-observability-sli/workspace"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **observability** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-systems-performance-profile",
        "id": 73,
        "title": "Performance & Profiling",
        "world_id": "world-python",
        "track_id": "python-systems",
        "description": "Implement most_common_tokens(text, k) to extract alphabetic tokens from text and return the top-k by frequency, sorted by count descending then alphabetically.",
        "short_description": "Implement most_common_tokens(text, k) to extract and rank alphabetic tokens from a text string.",
        "briefing_md": "Implement `most_common_tokens(text, k)`. Extract alphabetic sequences using `_TOKEN_RE`, lowercase them, count occurrences, and return the top-`k` as `[(token, count)]` sorted by count descending then token ascending on ties.",
        "summary": "Implement most_common_tokens(text, k) to extract alphabetic tokens from text and return the top-k by frequency, sorted by count descending then alphabetically.",
        "key_terms": [
            "glossary/python/iteration",
            "glossary/python/dictionary",
            "glossary/python/function"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "from __future__ import annotations\n\nfrom typing import Any\n\n\ndef count_hits(items: list[str], queries: list[str]) -> int:\n    \"\"\"\n    Return how many queries are present in items.\n    Use a set for membership checks (deterministic).\n    \"\"\"\n    # TODO\n    return 0\n\n\ndef naive_comparisons(items: list[str], queries: list[str]) -> int:\n    \"\"\"\n    Deterministic cost model for naive membership:\n    comparisons += number of item==query checks performed.\n    \"\"\"\n    # TODO\n    return 0\n\n\ndef set_ops(items: list[str], queries: list[str]) -> int:\n    \"\"\"\n    Deterministic cost model for set membership:\n    build_ops = len(items)\n    membership_ops = len(queries)\n    set_ops = build_ops + membership_ops\n    \"\"\"\n    # TODO\n    return 0\n\n\ndef choose_strategy(naive_cost: int, set_cost: int) -> str:\n    \"\"\"\n    Choose cheaper strategy. Tie-break: choose \"set\".\n    \"\"\"\n    # TODO\n    return \"set\"\n\n\ndef profile_membership_case(case: dict[str, Any]) -> dict:\n    \"\"\"\n    Pure function: read case dict and return report dict.\n\n    Required report shape:\n    {\n      \"hits\": int,\n      \"strategy\": \"naive\"|\"set\",\n      \"cost\": {\"naive_comparisons\": int, \"set_ops\": int}\n    }\n    \"\"\"\n    items = case.get(\"items\", [])\n    queries = case.get(\"queries\", [])\n\n    # Basic input validation (keep deterministic)\n    if not isinstance(items, list) or not all(isinstance(x, str) for x in items):\n        items = []\n    if not isinstance(queries, list) or not all(isinstance(x, str) for x in queries):\n        queries = []\n\n    ncost = naive_comparisons(items, queries)\n    scost = set_ops(items, queries)\n    strategy = choose_strategy(ncost, scost)\n    hits = count_hits(items, queries)\n\n    return {\n        \"hits\": hits,\n        \"strategy\": strategy,\n        \"cost\": {\n            \"naive_comparisons\": ncost,\n            \"set_ops\": scost,\n        },\n    }\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-systems-performance-profile/workspace"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **profiling** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    },
    {
        "slug": "python-systems-platform-tooling",
        "id": 74,
        "title": "Internal Tooling & DX",
        "world_id": "world-python",
        "track_id": "python-systems",
        "description": "Build pure utility functions for slugification, deduplication, and dispatching tool requests.",
        "short_description": "Build pure utility functions for slug generation, unique-sorted dedup, and a tool dispatcher.",
        "briefing_md": "Implement three pure utility functions — `slugify`, `unique_sorted`, and `run_tool_request` — that form the backbone of an internal developer toolbox. No I/O: inputs come in, results come out. Each function must pass deterministic tests covering edge cases.",
        "summary": "Build pure utility functions for slugification, deduplication, and dispatching tool requests.",
        "key_terms": [
            "glossary/python/function",
            "glossary/python/variable",
            "glossary/python/typing"
        ],
        "language": "python",
        "xp_base": 50,
        "starter_code": "from __future__ import annotations\n\nimport re\nfrom typing import Any\n\n\ndef _bad(tool: str, code: str) -> dict:\n    return {\"tool\": tool, \"ok\": False, \"result\": None, \"error\": code}\n\n\ndef _ok(tool: str, result: Any) -> dict:\n    return {\"tool\": tool, \"ok\": True, \"result\": result, \"error\": None}\n\n\ndef slugify(text: str) -> str:\n    \"\"\"\n    Rules:\n    - lowercase\n    - trim\n    - collapse whitespace to single spaces\n    - spaces -> \"-\"\n    - remove all chars except a-z, 0-9, \"-\"\n    - collapse multiple \"-\" to one\n    - strip leading/trailing \"-\"\n    \"\"\"\n    # TODO\n    return \"\"\n\n\ndef unique_sorted(items: list[str]) -> list[str]:\n    \"\"\"\n    - trim, lowercase\n    - drop empty\n    - unique + sorted\n    \"\"\"\n    # TODO\n    return []\n\n\ndef run_tool_request(req: dict) -> dict:\n    \"\"\"\n    Pure function: no IO.\n    \"\"\"\n    tool = req.get(\"tool\")\n    payload = req.get(\"payload\")\n\n    if not isinstance(tool, str) or not tool.strip():\n        return _bad(\"unknown\", \"EF_TOOL_BAD_INPUT\")\n\n    tool = tool.strip()\n\n    if not isinstance(payload, dict):\n        return _bad(tool, \"EF_TOOL_BAD_INPUT\")\n\n    if tool == \"slugify\":\n        text = payload.get(\"text\")\n        if not isinstance(text, str):\n            return _bad(tool, \"EF_TOOL_BAD_INPUT\")\n        return _ok(tool, slugify(text))\n\n    if tool == \"sum\":\n        nums = payload.get(\"numbers\")\n        if not isinstance(nums, list) or any(not isinstance(n, int) for n in nums):\n            return _bad(tool, \"EF_TOOL_BAD_INPUT\")\n        return _ok(tool, sum(nums))\n\n    if tool == \"unique_sorted\":\n        items = payload.get(\"items\")\n        if not isinstance(items, list) or any(not isinstance(x, str) for x in items):\n            return _bad(tool, \"EF_TOOL_BAD_INPUT\")\n        return _ok(tool, unique_sorted(items))\n\n    return _bad(tool, \"EF_TOOL_UNKNOWN\")\n",
        "objectives": [
            {
                "id": "tests_pass",
                "title": "Unit tests pass",
                "kind": "tests_pass"
            }
        ],
        "runtime_rules_json": {
            "require_exit_code_zero": true
        },
        "workspace": {
            "files_from": "../quests/python-systems-platform-tooling/workspace"
        },
        "tiered_hints": {
            "concept": "Review the workspace file structure.",
            "guided": "Review the concept of **virtual environment (venv)** in the Codex.",
            "full_solution": "No full solution provided. Check your syntax and ensure all tests pass."
        },
        "content_source": "generated_v1",
        "content_version": "9.9.0",
        "tier": 1
    }
]
