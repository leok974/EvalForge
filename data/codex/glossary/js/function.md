# function

## Definition
A function is reusable code that you can call by name. It can take parameters and return a value.

## Tiny example
```js
function add(a, b) {
  return a + b;
}
console.log(add(2, 3));
```

## Common pitfall
If you forget `return`, the function returns `undefined`.

## Related
parameter, return value
