---
id: glossary/sql/where
title: WHERE
world: sql
tags: [sql, fundamentals]
source: core
---

# WHERE

The `WHERE` clause is used to filter records, ensuring only those that fulfill a specified condition are returned.

## Example

```sql
-- Example usage for WHERE
SELECT * FROM users;
```
