---
id: glossary/sql/from
title: FROM
world: sql
tags: [sql, fundamentals]
source: core
---

# FROM

The `FROM` clause identifies which table the data should be pulled from.

## Example

```sql
-- Example usage for FROM
SELECT * FROM users;
```
