---
id: glossary/sql/select
title: SELECT
world: sql
tags: [sql, fundamentals]
source: core
---

# SELECT

The `SELECT` clause specifies which columns of data you want to retrieve from a database.

## Example

```sql
-- Example usage for SELECT
SELECT * FROM users;
```
