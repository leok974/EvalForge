---
id: glossary/sql/group-by
title: GROUP BY
world: sql
tags: [sql, fundamentals]
source: core
---

# GROUP BY

The `GROUP BY` statement groups rows that have the same values into summary rows.

## Example

```sql
-- Example usage for GROUP BY
SELECT * FROM users;
```
