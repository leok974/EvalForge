---
id: glossary/sql/order-by
title: ORDER BY
world: sql
tags: [sql, fundamentals]
source: core
---

# ORDER BY

The `ORDER BY` clause is used to sort the result-set in ascending or descending order.

## Example

```sql
-- Example usage for ORDER BY
SELECT * FROM users;
```
