---
id: glossary/sql/having
title: HAVING
world: sql
tags: [sql, fundamentals]
source: core
---

# HAVING

The `HAVING` clause was added to SQL because the `WHERE` keyword cannot be used with aggregate functions.

## Example

```sql
-- Example usage for HAVING
SELECT * FROM users;
```
