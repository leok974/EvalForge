# CSV

## Definition

CSV ("comma-separated values") is a common plain-text tabular format where each line is a row and commas separate columns. In Python you typically use the `csv` module to avoid edge cases like quoted commas and newlines.

## Tiny Example

```python
# Example code demonstrating csv
# TODO: Add concrete example
pass
```

## Common Pitfall

Watch out for common mistakes when using csv.

## Related

- Other Python terms (to be linked)

**Category:** runtime
