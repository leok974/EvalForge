# infinite loop

## Definition

An infinite loop is a loop that never stops because its exit condition is never reached (or doesn't exist). It's often caused by forgetting to update state, using the wrong condition, or accidentally reprocessing the same input.

## Tiny Example

```python
# Example code demonstrating infinite loop
# TODO: Add concrete example
pass
```

## Common Pitfall

Watch out for common mistakes when using infinite loop.

## Related

- Other Python terms (to be linked)

**Category:** debugging
