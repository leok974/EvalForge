# break / continue

## Definition

`break` exits the nearest loop immediately, while `continue` skips the rest of the current iteration and moves to the next one. They're useful for early exits and filtering, but can hide logic bugs if overused.

## Tiny Example

```python
# Example code demonstrating break / continue
# TODO: Add concrete example
pass
```

## Common Pitfall

Watch out for common mistakes when using break / continue.

## Related

- Other Python terms (to be linked)

**Category:** runtime
