# KeyError

## Definition

A `KeyError` happens when you try to access a dictionary key that doesn't exist (e.g., `d["missing"]`). Prevent it by using `in`, `dict.get()`, or by ensuring keys are created before access.

## Tiny Example

```python
# Example code demonstrating keyerror
# TODO: Add concrete example
pass
```

## Common Pitfall

Watch out for common mistakes when using keyerror.

## Related

- Other Python terms (to be linked)

**Category:** debugging
