{
  "name": "evalforge",
  "version": "1.0.0",
  "private": true,
  "description": "EvalForge - AI Trainer Arcade with multi-agent evaluation",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:smoke": "playwright test tests/e2e/devui-smoke.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0"
  }
}
