{
    "slug": "sql-where",
    "title": "Filter the Data",
    "language": "sql",
    "world_id": "world-sql",
    "track_id": "sql-fundamentals",
    "order_index": 3,
    "short_description": "Use the WHERE clause to filter rows by a condition.",
    "detailed_description": "Query only the rows you need by adding a WHERE clause. You will filter active users from the Archive and learn how boolean flags work in SQLite.",
    "key_terms": [
        "where",
        "filter",
        "is_active",
        "boolean"
    ],
    "objectives": [
        {
            "id": "sql_check",
            "kind": "tests_pass",
            "text": "Query returns only active users",
            "why": "WHERE clause filters correctly",
            "rule": {
                "must_pass": true
            }
        },
        {
            "id": "uses_where",
            "kind": "regex",
            "text": "Query uses WHERE clause",
            "why": "Enforces use of the target concept",
            "rule": {
                "pattern": "(?i)\\bWHERE\\b"
            }
        },
        {
            "id": "fs_snapshot",
            "kind": "fs_snapshot",
            "title": "Verify file structure",
            "rule": {
                "kind": "fs_snapshot",
                "must_exist": [
                    "README.md",
                    "fixtures/schema.sql",
                    "fixtures/seed.sql",
                    "task.sql"
                ]
            }
        }
    ],
    "grading": {
        "mode": "run"
    },
    "workspace": {
        "entrypoint": "task.sql",
        "files_from": "../quests/sql-where/workspace"
    },
    "smoke": {
        "solution_workspace_files": []
    },
    "content_source": "sql_world_v1",
    "content_version": "1.0.0",
    "runtime_rules_json": {
        "objectives": [
            {
                "id": "sql_check",
                "kind": "exit_code_zero",
                "text": "Query executes without errors",
                "why": "Ensure SQL syntax is correct",
                "rule": {
                    "kind": "exit_code_zero"
                }
            },
            {
                "id": "uses_where",
                "kind": "regex",
                "text": "Query uses WHERE clause",
                "why": "Enforces use of target concept",
                "rule": {
                    "pattern": "(?i)\\bWHERE\\b"
                }
            }
        ]
    }
}