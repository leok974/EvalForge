#!/bin/sh
set -eu

mkdir -p outputs

# TODO:
# Produce outputs/top.txt with the top 2 most frequent names in fixtures/names.txt
# Format: "name count" (name first, count second), one per line.
# Use pipes. Do not use temporary files.

echo "TODO" > outputs/top.txt
