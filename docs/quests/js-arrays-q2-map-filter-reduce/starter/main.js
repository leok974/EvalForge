// Prism Lens: map / filter / reduce
// Implement the functions below without using for-loops.

export function doubleAll(nums) {
    // TODO: return a new array where every number is doubled
}

export function onlyEvens(nums) {
    // TODO: return a new array containing only even numbers
}

export function sum(nums) {
    // TODO: return the sum of all numbers in nums
}

// Simple demo (Run should print these)
console.log(doubleAll([1, 2, 3]));  // expected: [2, 4, 6]
console.log(onlyEvens([1, 2, 3, 4])); // expected: [2, 4]
console.log(sum([1, 2, 3])); // expected: 6
