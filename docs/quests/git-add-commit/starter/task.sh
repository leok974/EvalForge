#!/bin/sh
set -eu
# TODO:
# - Create sandbox/repo, init git main
# - Copy fixtures into repo
# - Create .gitignore ignoring *.log
# - git add ONLY greeting.txt config.json (not temp.log)
# - commit with exact message
# - write outputs/summary.json (canonical JSON, sorted keys not required but valid JSON)

echo "TODO"
