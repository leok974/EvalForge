import { loadUser } from "./db.js";

export async function getUserName(id) {
    // TODO:
    // 1. Call loadUser(id) safely.
    // 2. Return the user.name.
    // 3. If loadUser throws, catch the error and return "Guest".
    return "TODO";
}
