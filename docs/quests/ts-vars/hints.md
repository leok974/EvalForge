# Hints — TS Vars

## Hint 1
The tests check exact values, not just types.

## Hint 2
Use literal types in the Config type (3, 250, "dev").

## Hint 3
`assert.deepEqual` compares object contents — key names must match.
