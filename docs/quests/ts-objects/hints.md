# Hints — TS Objects

## Hint 1
A “plain object” is one whose prototype is `Object.prototype` (or null).

## Hint 2
Reject non-integers with `Number.isInteger`.

## Hint 3
Normalize headers by iterating `Object.entries(headers)` and building a new object.
