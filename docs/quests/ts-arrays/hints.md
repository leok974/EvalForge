# Hints — TS Arrays

## Hint 1
Use `Array.isArray` to safely narrow `unknown`.

## Hint 2
Use `Number.isFinite` to reject NaN and Infinity.

## Hint 3
Numeric sorting requires a comparator: `(a, b) => a - b`.

## Hint 4
Deduplicate after rounding.
