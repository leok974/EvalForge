import fs from "node:fs/promises";
import path from "node:path";

export async function processFile(fileName) {
    // TODO:
    // 1. Construct absolute path using process.cwd() and fileName via path.join
    // 2. Read the file content (utf-8)
    // 3. Convert content to uppercase
    // 4. Write new content to "output.txt" in the SAME directory as the input file
    throw new Error("Not implemented");
}
