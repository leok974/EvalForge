// TODO: Get these values from process.env
// 1. PORT: Default to 3000 if not set.
// 2. DB_URL: Required. Throw an error if missing.

export const config = {
    port: undefined,
    dbUrl: undefined
};
