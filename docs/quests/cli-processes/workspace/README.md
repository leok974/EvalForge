# Processes

Edit `task.sh`.

## Goal
Read `fixtures/ps.txt` and write `outputs/pids.txt` containing the PIDs whose COMMAND includes `python`.

## Rules
- Ignore the header line.
- Output only PIDs (one per line).
- Sort ascending.
- No extra stdout/stderr output.
