// TS Modules - main task
import { sum, toCents } from "./math.ts";

export function formatInvoiceTotal(lineTotalsDollars: number[]): string {
    // TODO: convert to cents, sum, and format as: `Total: <cents> cents`
    return "Total: 0 cents";
}
