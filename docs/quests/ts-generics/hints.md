# Hints — TS Generics

## Hint 1
`keyof T` means “all keys of T”.

## Hint 2
Use `Pick<T, K>` to express “subset of properties”.

## Hint 3
You may need a type assertion when building an object incrementally.
