---
id: glossary/sql/row
title: row
world: sql
---

# row

A **row** (also called a record or tuple) represents a single, implicitly structured data item in a [table](glossary/sql/table). 

Each row in a table contains the same columns, but with values specific to that individual item. For example, in a `users` table, one row would represent one specific user (like 'Alice').