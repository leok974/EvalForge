# Props

Props (short for properties) are read-only. A component must never modify its own props.

## Passing Props

```jsx
<Welcome name="Sara" />
```

## Unidirectional Data Flow

Data flows down from parent components to child components via props.
