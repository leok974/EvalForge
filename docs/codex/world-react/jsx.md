# JSX

JSX is a syntax extension to JavaScript. It is valid JavaScript when compiled.

## Expressions

You can embed expressions in curly braces:

```jsx
const element = <h1>Hello, {name}</h1>;
```

## Children

JSX tags may contain children:

```jsx
<div>
  <h1>Hello!</h1>
</div>
```
