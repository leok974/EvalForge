# Performance Basics

## Memoization

*   `React.memo`: Higher order component for memoizing components.
*   `useMemo`: Hook for memoizing expensive calculations.
*   `useCallback`: Hook for memoizing function references.

## Virtual DOM

React uses a virtual DOM to minimize direct manipulation of the actual DOM, which is slow.
