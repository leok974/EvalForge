# EvalForge - Environment Variables Template
# Copy this to .env and fill in your values

# ===== Vertex AI Configuration =====
# Locked to gemini-2.5-flash in us-central1
# Project number is used for API calls (not project ID)
VERTEX_PROJECT_NUMBER=291179078777
VERTEX_PROJECT_ID=evalforge
VERTEX_REGION=us-central1
VERTEX_MODEL_ID=gemini-2.5-flash

# ===== DevDiag Configuration (HTTP Proxy) =====
# Base URL of DevDiag HTTP server (required for DevDiag diagnostics)
# Example: http://127.0.0.1:8080 (local) or https://devdiag.example.com (production)
# Leave empty to disable DevDiag features (will return 503 gracefully)
DEVDIAG_BASE=

# Bearer token for DevDiag authentication (optional)
# If DevDiag server requires authentication, set this to your JWT token
# Tokens are ONLY used server-side - never exposed to frontend
DEVDIAG_JWT=

# ===== Optional: Override defaults =====
# Uncomment to change server port (default: 19000)
# PORT=19000

# ===== Development =====
# Uncomment for debug logging
# DEBUG=true
